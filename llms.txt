# reclaimit

> Developer-aware disk cleanup CLI and terminal UI written in Go.

reclaimit finds regenerable developer artifacts, groups them by Git repository
or path, and lets users review candidates before deletion.

## Verified capabilities

- 17 cleanup categories covering JavaScript, Python, Rust, frontend builds,
  Bun, pip, pipx, generic caches, and macOS metadata.
- Plain-text, Markdown, and JSON reports.
- Interactive terminal UI.
- Read-only analysis and non-destructive cleanup dry runs.
- Pre-delete checks for path existence, type, measured size, and modification
  snapshot. Changed or missing candidates are skipped.
- Prebuilt release archives for Linux, macOS, and Windows.

## Install

```bash
curl -fsSL https://raw.githubusercontent.com/svg153/reclaimit/main/install.sh | bash

# Or with Go 1.24+
go install github.com/svg153/reclaimit/cmd/reclaimit@latest
```

## Example

```bash
reclaimit analyze --root "$HOME/code" --format json --out report.json
reclaimit clean --root "$HOME/code" --include-category python-venv --dry-run
```

## Safety

Deletion is irreversible and cannot be transactional on a normal filesystem.
Users should review dry-run output and keep backups for valuable data.

## Canonical links

- Website: https://svg153.github.io/reclaimit/
- Source: https://github.com/svg153/reclaimit
- Releases: https://github.com/svg153/reclaimit/releases/latest
- Documentation: https://github.com/svg153/reclaimit/tree/main/docs
- Issues: https://github.com/svg153/reclaimit/issues
- License: MIT
